# WEC v2.2.6-v2｜新华尊项目内部研发 PRD

## 关联需求｜REQ-0080｜新华尊 H5 免备案服务接入

飞书需求文档中的已梳理研发需求：

- 在前端 H5 配置处增加“是否收集用户信息”的选择设置。
- H5 链接支持传入加密用户标识，仅用于新华尊侧用户关联；不作为会员备案、权益校验或服务可用性条件。
- 报告面向用户推送，在报告页面增加单独的推送按钮，不设置自动推送节点。
- 本版本不调整现有服务发起配置。
- 服务回传数据包含基本信息和万能表单内容。

## 1. 本次研发内容

| 编号 | 类型 | 研发内容 |
| --- | --- | --- |
| R01 | H5 配置 | 在前端 H5 配置处增加“是否收集用户信息”选择设置。同时表示改设置对项目连接生效，对通用链接不生效 |
| R02 | H5 接入 | 支持 H5 链接携带加密用户标识（规则同国创项目） |
|  |
| R04 | 服务与工单信息 | 用户发起服务、创建工单时收集工单内用户信息；支持选择将该用户备案为会员。 |
| R05 | 材料收集 | 服务过程中需要补充材料时，在 H5 服务大厅和服务记录中以待办数量及红点提醒用户上传；PC 端支持直接复制材料上传链接并发送给用户。 |
| R06 | 数据回传 | 服务请求单、服务工单分别回传固定字段和万能表单字段；字段回传内容及回传时机由万能表单配置。 |
| R07 | 报告推送 | 在报告详情增加“推送报告”按钮；人工点击后将报告推送给 H5 端用户。 |
| R08 | 服务请求单万能表单 | 将服务请求单从固定字段改为可配置万能表单；万能表单配置增加“健康档案、工单、服务请求单”三类筛选；创建项目并配置前端 H5 时，服务表单可选择已配置的服务请求单万能表单。 |


## 2. 接口需求

### 2.1 H5 链接嵌入用户号

新华尊在睿聆 H5 链接中携带加密用户号；睿聆通过该用户号识别当前用户，并在服务请求、服务工单及数据回传中保留该标识。

### 2.2 服务信息回传

服务请求单、服务工单分别回传；每个阶段均包含固定字段和万能表单字段两部分。

#### 2.2.1 服务请求单回传

| 分类 | 字段 |
| --- | --- |
| 固定字段 | 服务请求单号、项目名称/项目编号、服务 ID、服务名称、服务请求状态、发起时间、新华尊加密用户号 |
| 万能表单字段 | 在万能表单中配置需要回传的字段、回传字段名称和回传时机。 |

#### 2.2.2 服务工单回传

| 分类 | 字段 |
| --- | --- |
| 固定字段 | 服务工单号、关联服务请求单号、项目名称/项目编号、服务 ID、服务名称、服务工单状态、创建时间、状态更新时间、新华尊加密用户号 |
| 万能表单字段 | 在万能表单中配置需要回传的字段、回传字段名称和回传时机。 |

#### 2.2.3 万能表单字段回传配置

| 配置项 | 规则 |
| --- | --- |
| 是否回传 | 选择“是”时，该字段随服务信息回传；选择“否”时不回传。 |
| 回传字段名称 | 配置该字段回传给新华尊时使用的字段名称。 |
| 回传阶段 | 下拉选择“服务请求单”或“服务工单”；字段仅在所选阶段回传。 |
| 字段回传时机 | 通过下拉选择服务状态；服务进入所选状态时回传该字段。 |

#### 2.2.4 状态对应表

| 阶段 | 睿聆状态 | 新华尊状态 | 说明 |
| --- | --- | --- | --- |
| 服务请求单 | 待处理 | 待受理 | 服务请求单进入待处理。 |
| 服务请求单 | 处理中 | 待受理 | 服务请求单处于处理中，仍按“待受理”回传。 |
| 服务请求单 | 已取消 | 已取消 | 服务请求取消后回传。 |
| 服务请求单 | 已转单 | 不回传 | 仅保留为睿聆内部状态。 |
| 服务工单 | 处理中 | 待预约 | 服务正在处理中，等待预约。 |
| 服务工单 | 预约成功（需新增） | 预约成功 | 睿聆新增状态后回传。 |
| 服务工单 | 已完成 | 已完成 | 服务完成后回传。 |
| 服务工单 | 已取消 | 已取消 | 服务取消后回传。 |
